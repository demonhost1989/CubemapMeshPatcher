Simple patcher that can set shader properties and texture paths of meshes en masse.
The goal right now is for it to add cubemaps to large number of meshes through a simple UI.
The current capabilites can be found in the SettingsTemplate.json.
